#pragma once
#include <string>

namespace duan{
    namespace strutil {

        // Function to convert a string to uppercase
        std::string upper(const std::string& s);
        // Function to convert a string to reversecase
        std::string reverse(const std::string& s);
    } // namespace strutil
}